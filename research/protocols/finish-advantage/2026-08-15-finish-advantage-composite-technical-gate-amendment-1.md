# Finish-advantage composite technical-gate protocol, amendment 1

Status: **prospectively frozen technical correction, outcome blind**

Recorded: 2026-08-15 (America/New_York), before any composite technical-gate or competitive launch

This amendment corrects one overbroad sentence in `2026-08-15-finish-advantage-composite-technical-gate-protocol-v1.md`. It changes no map, country, slot, seed, run count, policy-selection rule, exposure threshold, or outcome boundary.

Per-cell requirement 3 is replaced by:

> Every action-bearing finish-advantage decision has `enemyBuildingCount > 1`. When `enemyBuildingCount <= 1`, the finish-advantage layer must emit only an inactive, action-free handoff record. Every V5 action-bearing decision retains the exact frozen V5 schema, mechanism, information-boundary, and policy identity.

The inactive final-building record is required evidence for the intended composite architecture: exact Supalosa runs first, the multi-building layer yields, and unchanged V5 has exclusive authority over the final building. Treating that record as a violation would incorrectly reject the implementation the gate is meant to verify.

