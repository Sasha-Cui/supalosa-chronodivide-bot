# Neutral-building probe: prospective asset materialization repair A1

The v1 array 24639432 failed in all eight tasks at cdapi.init while opening
language.mix; finalizer 24639433 was cancelled. The pinned filesystem adapter
uses lstat().isFile() and rejects symlinks as TypeMismatchError. This is an
input materialization incompatibility, not a permission denial. No game was
created and no game update or competitive outcome was produced.

Preserve the entire v1 tree, manifest, dispatch records, and all error logs.
Do not overwrite or replace any v1 artifact.

## Narrow repair and mandatory compatibility check

Materialize the SAME byte-hashed source asset entries as independent regular
files in a NEW root:
research-evidence/live-building-ledger/neutral-probe-v1-materialization-a1.
Generate byte-identical fixture maps using the original frozen template and
rule modifications. Assert that every asset is a regular file and hash-equal
to the original. Do not alter installed assets or bypass the file reader.

Before any trace, run exactly one pi_jss233 day initialization-only job using
cdapi.init on the repaired asset directory. It must pass with zero game
instances and zero updates, matching source/program/protocol/runtime/asset
identities and a checksum-protected completion marker. After its Slurm
COMPLETED 0:0 and immutable output pass, submit exactly the original eight
technical cases and afterok finalizer. No automatic retry; no source edit
while any source-bound stage runs.

## Unchanged design and limits

All eight assignments, seeds, maps, actors, action schedule, 6000-update
horizon, determinism tests, outcome prohibitions, and technical gates remain
as frozen in 2026-09-03-neutral-building-lifecycle-probe-v1.md (f6dce6b).
Only asset file type, separate output root, a prerequisite initialization
check, and compact technical error logging change. This repair does not
constitute selective replay: the prior attempt never created a game, and the
full eight-case technical design is retained, not filtered by outcomes.

Resources: one initialization-only task, then at most eight concurrent
one-CPU/4-GiB/30-minute traces and one fail-closed finalizer, all pi_jss233 day.
A pass remains limited to neutral-building lifecycle and cannot promote the
competitive endpoint. Combatant-owned and negative lifecycle fixtures remain
required. Preserve every failure and every original scientific result.
