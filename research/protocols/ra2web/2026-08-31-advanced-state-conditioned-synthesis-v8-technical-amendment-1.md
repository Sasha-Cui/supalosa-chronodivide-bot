# Advanced state-conditioned synthesis V8 technical amendment 1

Status: **prospectively frozen before repaired traces**

Date frozen: 2026-08-31

## Reason

The V8 technical array completed 233 of 234 cells, but required
`advanced-recover` task 36 terminated generically before the 12,000-update
outcome-blind horizon. No orientation or endpoint was emitted or inspected.

The original V7 technical evidence established that all comparable games reach
9,600 updates without early termination. This amendment changes only the fixed
technical horizon from 12,000 to 9,600 updates.

## Immutable elements

The following remain byte-for-byte or definition-for-definition unchanged:

- the 1,620-case master selection and its SHA-256
  `9730745e171ecc73531369ddfd8308700eec44a7586491852713806fb097f713`;
- all 18 technical cases;
- all six fixtures, both opponent branches, and the plain Supalosa control;
- all 234 task assignments;
- V8 grammar, detector threshold/update, ownership seam, policies, source
  opponent identities, runtime, map, snapshots, audits, and pass criteria;
- absence of W/D/L, score, defeated side, terminal building count, endpoint
  orientation, and ranking; and
- CPU-only `pi_jss233` scheduling and complete-aggregate rule.

The repair does not modify any later competitive horizon or success gate.

## Pre-array probes

Before the repaired array, run and preserve two separate single-task technical
probes at 9,600 updates:

1. task 0: `advanced-fallback_only`, case 0; and
2. task 36: `advanced-recover`, case 0.

Each must reach exactly 9,600 updates with synchronized candidate/opponent
snapshots, correct update-1,200 Advanced detection, no ownership leak,
deterministic repeat hashes, and zero prohibited field. A generic early finish
stops the repair without exposing orientation.

## Repaired gate

After both probes pass, launch all tasks `0-233` again at concurrency at most
64 with a new result root and `afterok` fail-closed finalizer. Do not reuse any
V1 completed cell. Do not inspect partial cells.

The finalizer applies every original technical gate at update 9,600. Only a
complete passing aggregate may authorize competitive Generation 0.
