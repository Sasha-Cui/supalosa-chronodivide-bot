# Terminal-objective open-development replacement protocol, version 1

Status: **frozen before replacement generation or gameplay**

Frozen: 2026-08-13 UTC

Array `22119584` and controller `22119585` are invalidated in full by the
runtime-commitment incident recorded at
`research-evidence/terminal-objective/open-development-v1/failed-campaign-22119584.json`
(SHA-256 `ab44c5d7f6064335fbc34532abcc14e7178b97224e375a1fc64b610019f66514`).
No outcome was inspected. None of its 532 counted launches, 493 completed
episodes, 34 complete shards, partial events, summaries, or scheduler attempts
may be analyzed, selected, combined, reused, or reported.

The repair is one complete replacement of the original 1,800-game block. It
keeps exactly the original ten outcome-free selected families, two seed blocks,
all nine country mirrors, reciprocal slots, five policy arms in their original
order, 24,000-tick cap, literal endpoint v5, information boundary, uncertainty,
and advancement checks. Source changes are restricted to:

1. binding the failure audit and predecessor job IDs;
2. declaring zero reuse and a complete replacement;
3. changing the engine-seed base from `3,750,000,000` to the disjoint domain
   `3,760,000,000`; and
4. adding the already-frozen conditional fresh-development implementation and
   tests before replacement generation, so the checkout need not change while
   the replacement is active.

The replacement has 180 shards and 1,800 new games. It uses only Slurm account
`pi_jss233`, has no outcome-bearing retry, and advances only through the same
fail-closed controller and prespecified aggregate gate as the invalidated
campaign. Any replacement failure invalidates the complete replacement again;
successful or outcome-bearing shards are never reused selectively.
